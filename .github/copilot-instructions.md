# Copilot Instructions

## Project Guidelines
- User prefers starting with basic backend smoke checks for frontend implementation confidence, then later evolving tests to more robust unit tests using mocks and interfaces, plus endpoint tests for API behavior.
- User prefers direct, task-specific guidance and does not want basic instructions they already know.